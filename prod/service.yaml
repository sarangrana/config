apiVersion: v1
kind: Service
metadata:
  name: jirautil
  namespace: prod
  labels:
    app: jirautil
spec:
  ports:
    - port: 80
  selector:
    app: jirautil
    tier: jirautil
  type: LoadBalancer
